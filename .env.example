# Penpot personal access token (get one from Penpot > Settings > Access tokens)
PENPOT_TOKEN=

# Prisma — koneksi Postgres (lihat prisma/schema.prisma)
DATABASE_URL=postgresql://user:password@localhost:5432/task_manager_dev
